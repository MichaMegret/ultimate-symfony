{% extends "base.html.twig" %}

{# {% form_theme formView _self %} ==> Pour utiliser le theme predefini dans un fichier, ici _self, voir twig.yaml pour definition global #}

{% block title %}Nouveau produit{% endblock %}

{% block body %}
    <h1>Nouveau produit</h1>

    {#{form(formView)}#}

    {{form_start(formView)}}
    
    {{form_errors(formView)}}

    <div class="row">
        <div class="col">
            {# <div class="nameContaner">
                {{form_label(formView.name)}}
                {{form_errors(formView.name)}}
                {{form_widget(formView.name)}}
                {{form_help(formView.name)}}
            </div> #}
            {{form_row(formView.name)}}
            {{form_row(formView.price)}}
        </div>
        <div class="col">
            {{form_row(formView.category)}}        
            {{form_row(formView.shortDescription)}}
            {{form_row(formView.mainPicture)}}
        </div>
    </div>
    
    {#{form_widget(formView)}#}

    {#{form_row(formView.name)}#}

    
    <button type="submit" class='btn btn-primary mt-4'>
        <i class="fas fa-save"></i>
        Ajouter le produit
    </button>

    {{form_end(formView)}}

{% endblock %}


{# {% block form_row %}
    <div class="form-group mt-2 mb-2">
        {{ form_label(form) }}
        {{ form_widget(form, {"attr": {"class": "form-control"} }) }}
        {{ form_help(form) }}
        {{ form_errors(form) }}
    </div>
{% endblock %} #}